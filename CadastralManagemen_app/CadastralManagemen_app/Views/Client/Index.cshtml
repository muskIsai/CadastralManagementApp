@model IEnumerable<CadastralManagemen_app.Models.Client>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Users.fullname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.passportNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Users.fullname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.passportNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDclient }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDclient }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDclient })
        </td>
    </tr>
}

</table>
